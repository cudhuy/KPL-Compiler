PROGRAM Exercise1;

VAR a : INTEGER; b: INTEGER;
BEGIN

a := readi;
b := readi;
BEGIN
  IF a <= b THEN
    BEGIN
    CALL WriteI(b-a-1);
    CALL WriteLn;
    END
  ELSE
    BEGIN
    CALL WriteI(a-b-1);
    CALL WriteLn;
    END
END
END.